apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ss2mqtt.fullname" . }}-test-connection"
  labels:
    {{- include "ss2mqtt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ss2mqtt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
