apiVersion: v1
kind: Service
metadata:
  name: {{ include "ss2mqtt.fullname" . }}
  labels:
    {{- include "ss2mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ss2mqtt.selectorLabels" . | nindent 4 }}
